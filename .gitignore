node_modules/
.next/
dist/
build/

# Environment - never commit secrets
.env
.env.local
.env.*.local

# License key directory - private key must never be committed.
# The public key is embedded in src/lib/license.ts; the file here is just a reference copy.
# Issuing tool lives in cyberrisk-license-issuer (separate private repo)
keys/
!keys/README.md

# Claude Code local config and memory
.claude/

# Logs
*.log
package-lock.json
npm-debug.log*
yarn-debug.log*

# OS
.DS_Store
Thumbs.db

# Yarn
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# TypeScript
tsconfig.tsbuildinfo
next-env.d.ts

# User-uploaded documents (runtime data, not source)
data/documents/

memory/
# Prisma generated client
src/generated/
